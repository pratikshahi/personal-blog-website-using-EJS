<%- include("partials/header") %>
    <h1>Its HOME</h1>
    <p>
        <%= homeText %>
    </p>
    <% posts.forEach(function(text){ %>
        <h1>
            <%= text.title %>
        </h1>
        <p>
            <%= text.postText.substring(0,100)+"..."%> <a href="/post/<%= text.title %> ">Read More</a>
        </p>


        <% });%>
            <%- include("partials/footer") %>